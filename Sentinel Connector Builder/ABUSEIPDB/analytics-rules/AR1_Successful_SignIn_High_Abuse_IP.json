{
    "id": "AbuseIPDB-AR1-Successful-SignIn-High-Abuse-IP",
    "name": "AbuseIPDB - Successful Sign-in from High Confidence Abusive IP",
    "description": "Triggers when a user successfully authenticates (Interactive, Non-Interactive, or ADFS) from an IP address with an AbuseIPDB confidence score >= 80. Covers all user sign-in types to detect compromised credentials, token replay, VPN misuse, or credential stuffing success.",
    "severity": "High",
    "enabled": true,
    "tactics": ["InitialAccess"],
    "techniques": ["T1078"],
    "query": "let HighConfidenceIPs = AbuseIPDB_Blacklist_CL\n| where TimeGenerated > ago(1d)\n| where AbuseConfidenceScore >= 80\n| summarize AbuseScore = max(AbuseConfidenceScore), Country = any(CountryCode) by IpAddress;\nunion\n(SigninLogs | where TimeGenerated > ago(1h) | where ResultType == 0 | project TimeGenerated, UserPrincipalName, IPAddress, AppDisplayName, SignInType = 'Interactive'),\n(AADNonInteractiveUserSignInLogs | where TimeGenerated > ago(1h) | where ResultType == 0 | project TimeGenerated, UserPrincipalName, IPAddress, AppDisplayName, SignInType = 'NonInteractive'),\n(ADFSSignInLogs | where TimeGenerated > ago(1h) | where ResultType == 0 | project TimeGenerated, UserPrincipalName, IPAddress, AppDisplayName = ResourceDisplayName, SignInType = 'ADFS')\n| where IPAddress in ((HighConfidenceIPs | project IpAddress))\n| join kind=inner HighConfidenceIPs on $left.IPAddress == $right.IpAddress\n| project TimeGenerated, UserPrincipalName, IPAddress, AbuseScore, Country, AppDisplayName, SignInType\n| extend AccountName = tostring(split(UserPrincipalName, \"@\")[0]), AccountUPNSuffix = tostring(split(UserPrincipalName, \"@\")[1])",
    "queryFrequency": "PT1H",
    "queryPeriod": "P1D",
    "triggerOperator": "GreaterThan",
    "triggerThreshold": 0,
    "suppressionDuration": "PT5H",
    "suppressionEnabled": false,
    "entityMappings": [
        {
            "entityType": "Account",
            "fieldMappings": [
                { "identifier": "Name", "columnName": "AccountName" },
                { "identifier": "UPNSuffix", "columnName": "AccountUPNSuffix" }
            ]
        },
        {
            "entityType": "IP",
            "fieldMappings": [
                { "identifier": "Address", "columnName": "IPAddress" }
            ]
        }
    ],
    "requiredDataConnectors": [
        { "connectorId": "AbuseIPDBConnectorDefinition", "dataTypes": ["AbuseIPDB_Blacklist_CL"] },
        { "connectorId": "AzureActiveDirectory", "dataTypes": ["SigninLogs", "AADNonInteractiveUserSignInLogs"] },
        { "connectorId": "ADFSConnector", "dataTypes": ["ADFSSignInLogs"] }
    ]
}
