{
    "id": "AbuseIPDB-HQ2-Network-Connections-Blacklisted-IPs",
    "name": "AbuseIPDB - Device Network Connections to Blacklisted IPs",
    "description": "Correlates AbuseIPDB high-confidence blacklisted IPs with Defender for Endpoint network events to identify devices communicating with known abusive IPs. Focuses on IPs with abuse confidence score >= 75 to reduce noise.",
    "severity": "High",
    "tactics": ["CommandAndControl", "Exfiltration"],
    "techniques": ["T1071", "T1041"],
    "query": "let BlacklistedIPs = AbuseIPDB_Blacklist_CL\n| where TimeGenerated > ago(1d)\n| where AbuseConfidenceScore >= 75\n| summarize AbuseScore = max(AbuseConfidenceScore), Country = any(CountryCode) by IpAddress;\nDeviceNetworkEvents\n| where Timestamp > ago(14d)\n| where RemoteIP in ((BlacklistedIPs | project IpAddress))\n| join kind=inner BlacklistedIPs on $left.RemoteIP == $right.IpAddress\n| project Timestamp, DeviceName, RemoteIP, RemotePort, AbuseScore, Country, InitiatingProcessFileName, InitiatingProcessCommandLine, ActionType\n| order by AbuseScore desc, Timestamp desc",
    "requiredDataConnectors": [
        { "connectorId": "AbuseIPDBConnectorDefinition", "dataTypes": ["AbuseIPDB_Blacklist_CL"] },
        { "connectorId": "MicrosoftThreatProtection", "dataTypes": ["DeviceNetworkEvents"] }
    ]
}
